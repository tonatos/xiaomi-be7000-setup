#!/bin/sh
# Откат правил маршрутизации mihomo. Generated by xiaomi-be7000-setup.
# Использование: sh rollback.sh

MIHOMO_DIR="{{ stack_path }}/mihomo"
ROUTING_SCRIPT="$MIHOMO_DIR/mihomo-routing.sh"

if [ -x "$ROUTING_SCRIPT" ]; then
    sh "$ROUTING_SCRIPT" stop
    logger -t mihomo-rollback "Правила маршрутизации mihomo сброшены"
else
    logger -t mihomo-rollback "Скрипт $ROUTING_SCRIPT не найден или не исполняемый"
    exit 1
fi
